# Narrows the private `client` to riffer's Gemini transport, mirroring the
# SDK-typed narrowings alongside it.
class Riffer::Providers::Gemini
  private

  def client: (?model: String?) -> Riffer::Providers::Gemini::Client
end
