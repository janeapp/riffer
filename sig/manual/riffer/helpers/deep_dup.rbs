# `Riffer::Helpers::DeepDup` uses `extend self`; rbs-inline doesn't emit that,
# so re-extend here to expose its instance methods as singleton methods.
module Riffer::Helpers::DeepDup
  extend ::Riffer::Helpers::DeepDup
end
