# `Riffer::Helpers::Validate` uses `extend self`; rbs-inline doesn't emit that,
# so re-extend here to expose its instance methods as singleton methods.
module Riffer::Helpers::Validate
  extend ::Riffer::Helpers::Validate
end
